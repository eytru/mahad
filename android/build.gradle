buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Add this line to update the Android Gradle Plugin version
        classpath 'com.android.tools.build:gradle:8.8.0'  // Or higher version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}